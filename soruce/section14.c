#include <stdio.h>
#include <string.h>
int main(){

    //1 지역변수 -> 스택, 전역변수 -> 스택, 전역변수,정젹변수-> 데이터영역, 코드-> 코드영역
    //2 스택영역에는 지역변수와 함수에 전달되는 인자, 즉 매개변수의 값이 저장
    //  스택 영역에는 함수 내에섬나 유효한 데이터가 저장된다. 이런 데이터들은 함수가 종료되면 사라지는 특징이있다.

}